<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>
    <h1>{{ title }}</h1>



    <table border="1" cellspacing="0" cellpadding="5">
        <thead>
            <tr>
                <th>Stage</th>
                <th>Home Team</th>
                <th>Home Goals</th>
                <th>Away Team</th>
                <th>Away Goals</th>
                <th>Win Conditions</th>
            </tr>
        </thead>
        <tbody>
            {% for match in matches %}
            <tr>
                <td>{{ match['Stage'] }}</td>
                <td>{{ match['Home Team Name'] }}</td>
                <td>{{ match['Home Team Goals'] }}</td>
                <td>{{ match['Away Team Name'] }}</td>
                <td>{{ match['Away Team Goals'] }}</td>
                <td>{{ match['Win conditions'] }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    {% if champion %}
        <p>Champion: {{ champion }}</p>
        {% if photo %}
            <img src="{{ photo }}" alt="Champion Team Photo" style="width: 1000px; height: auto;">
        {% endif %}
    {% endif %}
</body>
</html>
